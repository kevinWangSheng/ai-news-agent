# 011 · Digest — Tasks

> 008 一并写完前端;LLM-side digest 生成留给 012-scheduler。

- [x] digest/page.tsx — 列已有 digest;空态文案指向 scheduler 06:30
- [ ] LLM-driven intro + item 选择 — 推迟到 012 落地 `digest_daily/weekly` job 时实现;`POST /api/digests/generate` 已就绪
- [x] **verify** `pnpm build` 通过
